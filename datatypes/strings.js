const name = "Hamza"
const city = "Bangalore"

console.log(`Hello My name is ${name} and I am from ${city}`);


let url = "https//google.com"
console.log(url.length);  // This method is used to calculate the length of string
console.log(url.replace('google','habibi')); // This method is used to replace a string 
                                             //  with another name or string
console.log(url.indexOf('g')); //This method is used to check the index of a particular string
console.log(url.charAt(2)); // This method is used to check the position of a specific string 


let gameName = "  BGMI   "
console.log(gameName.trim()); // This function is used to cut all the whitespaces and also it has 2 function trimstart and trimend

console.log(gameName.includes("bgmi"));
console.log(gameName.includes("BGMI")); // This function is used to check the given string is available or not

